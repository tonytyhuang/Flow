const Exercise = (weatherInfo) => {
    const temperature = weatherInfo[0].temp;
    const weather = weatherInfo[0].weather[0].main;
    const outdoor = ['Run', 'Sports', 'Gym', 'Home Exercise' ]

}

export default Exercise;